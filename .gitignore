# PyLCSS - Professional Low-Code System Solutions
# .gitignore file

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# PyInstaller
*.manifest
*.spec

# Virtual environments
.venv/
venv/
ENV/
env/

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Data files
*.h5
*.hdf5
*.csv

# Logs
*.log
logs/

# Temporary files
*.tmp
*.temp
temp/
tmp/

# Encryption salt
pylcss/assistant_systems/config/.llm_key

# Distribution
dist/
build/

# External solver binaries (CalculiX, OpenRadioss) fetched by
# scripts/install_solvers.py — large, platform-specific, and reproducible
# via the script, so don't commit them or the auto-generated path config.
external_solvers/

# Downloaded benchmark decks (e.g. the ~30 MB Neon HPC zip).  Fetched by
# `python scripts/install_solvers.py --with-neon` and reproducible from the
# upstream URL, so we don't commit them.
data/benchmarks/neon/